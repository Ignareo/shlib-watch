{
  "name": "book-borrow-tracker-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "图书难借吗 · 安卓 App（Capacitor 壳 + sampler 同构核心）",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/filesystem": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/cli": "^7.4.0",
    "esbuild": "^0.25.0"
  }
}
